if (num > 0) {
    console.log("დადებითი");
} else if (num < 0) {
    console.log("უარყოფითი");
} else {
    console.log("ნული");
}

function isEven(num) {
    return num % 2 === 0;
  }

//მასივები გვჭირდება იმისთვის, რომ ერთ ცვლადში შევინახოთ ბევრი მონაცემი.

//let -შეგიძლია მთლიანად შეცვალო მასივი

//const საკვანძო სიტყვით განსაზღვრული ცვლადის ხელახლა მინიჭება შეუძლებელია

//push() ამატებს ელემენტს ბოლოში

//pop()შლის ბოლო ელემენტს

//unshift()ამატებს ელემენტს დასაწყისშ