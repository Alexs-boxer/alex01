const masive = [3, 5, 7, 9, 11];
const result = masive.map(item => item * 2);
console.log(result);

const arr = [10, 20, 30, 40];
const newArr = arr.map(item => "ID: " + item);
console.log(newArr);

let numbers = [1, 2, 3, 4];

let filtered = myFilter(numbers, num => num > 2);

console.log(filtered);